import 'package:flutter/material.dart';

class MusiumScreen extends StatelessWidget {
  const MusiumScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        // child: GestureDetector(onHorizontalDragEnd: ),
        );
  }
}
